{
  "name": "crm-max",
  "version": "1.0.0",
  "description": "CRM система для записи клиентов в мессенджере MAX",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../bot && npm install && cd ../telegram-bot && npm install",
    "bot": "cd bot && npm run dev",
    "telegram-bot": "cd telegram-bot && npm run dev"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}